apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nvmeof-external-volume
spec:
  storageClassName: ceph-nvmeof
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 128Mi
